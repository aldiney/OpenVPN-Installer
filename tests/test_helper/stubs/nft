#!/usr/bin/env bash
# Stub do 'nft' para os testes. Registra os argumentos; não altera o firewall.
printf '%s\n' "$*" >> "${STUB_CALLS_DIR:?STUB_CALLS_DIR não definido}/nft"
exit "${STUB_NFT_EXIT:-0}"
